[project]
name = "test-app"
version = "0.1.0"
description = "dbt project for computing Weighted Average Carbon Intensity (WACI) for portfolios"
readme = "README.md"
requires-python = ">=3.9"
dependencies = [
    "dbt-duckdb>=1.9",
    "pyarrow>=18.0",
]
