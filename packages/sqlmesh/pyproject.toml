[project]
name = "waci-sqlmesh"
version = "0.1.0"
description = "SQLMesh implementation of the WACI pipeline"
requires-python = ">=3.12"
dependencies = [
    "sqlmesh[duckdb]>=0.142",
    "pyarrow>=19.0",
]
