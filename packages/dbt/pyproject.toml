[project]
name = "analytics-dbt"
version = "0.1.0"
description = "dbt implementation of the analytics pipeline"
requires-python = ">=3.12"
dependencies = [
    "dbt-duckdb>=1.10",
    "pyarrow>=19.0",
]
