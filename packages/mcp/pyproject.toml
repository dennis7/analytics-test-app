[project]
name = "waci-mcp"
version = "0.1.0"
description = "MCP server for conversational exploration of WACI pipeline data"
requires-python = ">=3.12"
dependencies = [
    "mcp>=1.0",
    "duckdb>=1.0",
]
